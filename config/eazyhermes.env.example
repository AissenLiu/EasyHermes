# Copy to .env next to start-docker.bat if you want Docker Compose to load it.
# Keep the default localhost binding unless you set a password.

EAZYHERMES_WEBUI_PORT=8787
EAZYHERMES_START_GATEWAY=0

# Required before exposing WebUI beyond localhost.
# HERMES_WEBUI_PASSWORD=change-me

